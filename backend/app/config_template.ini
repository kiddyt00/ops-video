[app]
# Project settings
PROJECT_NAME=Ops-Video
VERSION=0.1.0

# Database
database_url=%(DATABASE_URL)s

# Image Generation (通义万相 DashScope)
dashscope_api_key=%(DASHSCOPE_API_KEY)s
dashscope_model=%(DASHSCOPE_MODEL)s
image_provider=%(IMAGE_PROVIDER)s

# Storage
storage_path=%(STORAGE_PATH)s

# CORS
frontend_url=%(FRONTEND_URL)s
